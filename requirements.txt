# Autolauncher - Python Dependencies
# Install with: pip install -r requirements.txt

# PyQt6 - Qt6 bindings for Python (cross-platform GUI framework)
PyQt6>=6.5.0

# PyQt6-Fluent-Widgets - Microsoft Fluent Design widgets library
# PINNED: v1.8.1 crash was caused by missing FluentIcon.MOON in unpinned version
PyQt6-Fluent-Widgets~=1.9.2

# APScheduler - Advanced Python Scheduler for task scheduling
APScheduler>=3.10.0

# Optional: PyInstaller for creating standalone executables
pyinstaller>=5.13.0

# Additional dependencies
pywinauto
pypiwin32
pyautogui
Pillow
GPUtil

# Requests - HTTP library for checking updates
requests>=2.31.0

# psutil - System and process utilities (required for system monitoring)
psutil>=5.9.0
